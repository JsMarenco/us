// Third-party dependencies

// Current project dependencies

const base = "/api/generate";

const generateEndpoints = {
  images: {
    create: {
      topic: `${base}/images/topic`,
    },
  },
};

export default generateEndpoints;
